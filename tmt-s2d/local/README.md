# Local preview (no Google)

From the repo:

```bash
cd tmt-s2d/local
python3 -m http.server 8765
```

Open http://127.0.0.1:8765/

| Role | Email | Password |
|---|---|---|
| Shop (DNM Flora demo) | demo@dnmflora.test | Demo#1234 |
| Platform admin | training@sheetomatic.in | Admin#1234 |

Data is in the browser (`localStorage`). Use **Reset data** on the orange banner to start over.

This is the same UI as the Apps Script app. WhatsApp / Razorpay are mocked (quote text is still generated).
